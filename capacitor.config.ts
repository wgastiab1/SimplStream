import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.justanormalchurro.wilstream',
  appName: 'WilStream',
  webDir: 'dist'
};

export default config;
